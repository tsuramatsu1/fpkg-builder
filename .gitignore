# Packages and build artefacts
*.pkg
*.pkg.remastered.pkg
*.naps_metric.json
*.gp5
*.playgo-scenario.json
.gp5-assets/
*-build-logs/

# The SDK binaries are not redistributable; the scripts that drive them live here
# but the binaries are found at run time via PS5_FPKG_TOOLKIT or -ToolkitRoot.
toolchain/
prospero-pub-cmd.exe
libScePubTools.dll
prospero-dds2png.exe

# Game dumps and backport payloads
*.sprx
*.prx
*.esbak
eboot.bin

__pycache__/
*.pyc
